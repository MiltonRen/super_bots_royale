<%= form_with(model: arena, class: "max-w-sm") do |form| %>
  <% if arena.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(arena.errors.count, "error") %> prohibited this arena from being saved:</h2>

      <ul>
        <% arena.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: "w-full input bg-white" %>
  </div>

  <%= form.submit "SUBMIT", class: "btn btn-primary w-full justify-start mt-4" %>
<% end %>
